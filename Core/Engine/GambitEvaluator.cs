using Autobattle.Core.Models;
using System.Collections.Generic;

namespace Autobattle.Core.Engine
{
    public static class GambitEvaluator
    {
        // Evaluates a unit's gambit chain and returns the chosen action (or null if none match)
        public static string? Evaluate(Unit unit, BattleState state)
        {
            // TODO: Implement chain of responsibility for gambit slots
            return null;
        }
    }
}